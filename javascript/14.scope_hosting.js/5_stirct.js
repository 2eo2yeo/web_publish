// strict mode: 엄격 모드 :: NodeJS 기반 사용
// react는 use strict 모드가 기본, strict 모드에서는 키워드가 (let, const) 생략되면 안됨
"use strict"; //  NodeJS에서 키워드를 정확히 체크하기 위함 

const a = 10; // var a = 10;
console.log(`a :: ${a}`);

